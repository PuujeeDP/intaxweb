@extends('frontend.layouts.app')

@php
    $metaTitle = 'Blog Posts' . ($search ? ' - Search: ' . $search : '') . ' | ' . config('app.name');
    $metaDescription = 'Browse our latest blog posts' . ($search ? ' for "' . $search . '"' : '');
@endphp

@section('content')
{{-- Page Header --}}
<section class="bg-gradient-to-r from-red-600 to-red-700 text-white py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h1 class="text-4xl md:text-5xl font-bold mb-4">
            @if($search)
                Search Results for "{{ $search }}"
            @else
                Blog Posts
            @endif
        </h1>
        <p class="text-xl text-red-100">
            @if($search)
                Found {{ $posts->total() }} {{ Str::plural('post', $posts->total()) }}
            @else
                Discover our latest articles and insights
            @endif
        </p>
    </div>
</section>

{{-- Posts Grid --}}
<section class="py-12 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        @if($posts->count() > 0)
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12">
                @foreach($posts as $post)
                <article class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-xl transition-shadow duration-300">
                    @if($post->featuredImage)
                    <a href="{{ localized_route('posts.show', ['slug' => $post->slug]) }}" class="block">
                        <img src="{{ asset('storage/' . $post->featuredImage->file_path) }}"
                             alt="{{ $post->title }}"
                             class="w-full h-48 object-cover">
                    </a>
                    @else
                    <a href="{{ localized_route('posts.show', ['slug' => $post->slug]) }}" class="block">
                        <div class="w-full h-48 bg-gradient-to-br from-red-500 to-red-700 flex items-center justify-center">
                            <svg class="w-16 h-16 text-white opacity-50" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"></path>
                            </svg>
                        </div>
                    </a>
                    @endif

                    <div class="p-6">
                        @if($post->category)
                        <a href="{{ localized_route('categories.show', ['slug' => $post->category->slug]) }}"
                           class="inline-block text-xs font-semibold text-red-600 hover:text-red-700 mb-2 uppercase tracking-wider bg-red-50 px-3 py-1 rounded-full">
                            {{ $post->category->name }}
                        </a>
                        @endif

                        <h2 class="text-xl font-bold text-gray-900 mb-2">
                            <a href="{{ localized_route('posts.show', ['slug' => $post->slug]) }}" class="hover:text-red-600 transition">
                                {{ $post->title }}
                            </a>
                        </h2>

                        @if($post->excerpt)
                        <p class="text-gray-600 mb-4 line-clamp-3">
                            {{ Str::limit($post->excerpt, 150) }}
                        </p>
                        @endif

                        <div class="flex items-center justify-between text-sm text-gray-500">
                            <span class="font-medium">{{ $post->author->name }}</span>
                            <time datetime="{{ $post->published_at?->format('Y-m-d') }}">
                                {{ $post->published_at?->format('M d, Y') }}
                            </time>
                        </div>

                        <a href="{{ localized_route('posts.show', ['slug' => $post->slug]) }}"
                           class="mt-4 inline-flex items-center text-red-600 hover:text-red-700 font-medium">
                            Read more
                            <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                            </svg>
                        </a>
                    </div>
                </article>
                @endforeach
            </div>

            {{-- Pagination --}}
            <div class="flex justify-center">
                {{ $posts->links() }}
            </div>
        @else
            {{-- Empty State --}}
            <div class="text-center py-16">
                <svg class="mx-auto h-24 w-24 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                </svg>
                <h3 class="mt-4 text-xl font-medium text-gray-900">No posts found</h3>
                <p class="mt-2 text-gray-600">
                    @if($search)
                        Try searching with different keywords
                    @else
                        Check back later for new content
                    @endif
                </p>
                @if($search)
                <a href="{{ localized_route('posts.index') }}" class="mt-6 inline-block bg-red-600 text-white px-6 py-3 rounded-lg hover:bg-red-700 transition shadow-lg">
                    View All Posts
                </a>
                @endif
            </div>
        @endif
    </div>
</section>
@endsection
